# Copy this file to .env.local (gitignored).

# Vite dev server proxies /jsonrpc.php to this URL so requests stay same-origin.
VITE_KANBOARD_URL=http://localhost:8080

# SSH target for `npm run kanboard:deploy`. Format: user@host
# Example: KANBOARD_HOST=pi@raspberrypi.local
KANBOARD_HOST=
